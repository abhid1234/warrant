# Arize AX — Settings → Space Settings & Keys
ARIZE_SPACE_ID=
ARIZE_API_KEY=
ARIZE_PROJECT_NAME=warrant-booker

# Optional: a real LLM call is traced when this is set; otherwise a templated
# confirmation is used (the manual AGENT/TOOL spans still export).
OPENAI_API_KEY=
