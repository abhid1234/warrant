{
  "name": "@warrant/demo",
  "version": "0.1.0",
  "description": "Two agents over A2A (one genuinely does the task, one lies) -> live green vs red warrants, plus a static public reputation board generated from the warrants.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "tsc -p tsconfig.json && node dist/demo/src/run.js",
    "cross-harness": "tsc -p tsconfig.json && node dist/demo/src/cross-harness.js",
    "a2a": "tsc -p tsconfig.json && node dist/demo/src/a2a-demo.js",
    "github": "tsc -p tsconfig.json && node dist/demo/src/github-verify.js",
    "board": "tsc -p tsconfig.json && node dist/demo/src/build-board.js"
  },
  "comment": "No dependencies — Node built-ins only (no external deps). Imports the sibling @warrant/verify by source path; both compile together.",
  "dependencies": {},
  "devDependencies": { "typescript": "*" }
}
